
To Do:
~~~~~~

 * Do a precompile actions and postcompile actions funcs to handle
   confs, caches, ...

 * List of files that will be compiled.
 * List of unit tests.
 * Validate settings before using them.
 * Avoid rescanning closure library folders continously.
 * Reuse deps.js instead of building a new one in /input/deps.js.

 * Build testing facilities to a folder in disk.
 * Better concurrency when disk seeks are common.
